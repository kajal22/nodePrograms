var name = "kajal";

function showName () {
    var name = "Jack"; // local variable; only accessible in this showName function
    me function
    console.log (name); // Jack
console.log (name); // kajal: the global variable
